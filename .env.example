# Copy this file to .env (not committed) and adjust values as needed.

# Google Gemini (images only). Leave empty if unused.
VITE_GEMINI_API_KEY=

# Optional: Override Ollama base URL (dev proxy already maps /ollama → http://localhost:11434)
# VITE_OLLAMA_BASE_URL=http://localhost:11434

# Preferred local storytelling/GM model
VITE_OLLAMA_MODEL=mothership-gm

